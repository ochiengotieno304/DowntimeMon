<%= simple_form_for([@service, @service.reports.build], html: { data: { modal: true } }) do |f| %>
  <!-- collection.build(attributes = {}, …) Returns one or more new objects of
  the collection type that have been instantiated with attributes and linked to
  this object through a foreign key, but have not yet been saved. Note: This
  only works if an associated object already exists, not if it‘s nil! -->

  <div class="field">
  <h1 class="text-xl font-bold">Add Maintenance Summary</h1>
    <div class="my-5">
      <%= f.input :maintainer, input_html: { class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' }, wrapper: false, label_html: { class: 'label' } %>
    </div>

    <div class="my-5">
      <%= f.input :reason, input_html: { class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' }, wrapper: false, label_html: { class: 'label' } %>
    </div>

    <div class="my-5">
      <%= f.input :duration, input_html: { class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' }, wrapper: false, label_html: { class: 'label' } %>
    </div>

    <div class="my-5">
      <% f.input :last_downtime, input_html: { class: 'block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full' }, wrapper: false, label_html: { class: 'label' } %>
    </div>
  </div>

  <%= f.button :submit, 'Add Summary', class: "rounded-lg py-3 px-5 bg-blue-900 text-white inline-block font-medium cursor-pointer" %>
<% end %>
